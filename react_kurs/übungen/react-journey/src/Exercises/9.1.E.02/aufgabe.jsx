// Aufgabe: Testing

// Erstelle eine React-Komponente WetterWidget, die die aktuelle Temperatur und den Wetterstatus (z.B. sonnig, bewölkt) anzeigt. 
// Verwende dabei Zustands-Hooks, um die Daten zu speichern. Anschließend schreibe mit Jest zwei Tests für diese Komponente:

// a) einen Test, der überprüft, ob die Komponente korrekt rendert, und
// b) einen Test, der überprüft, ob die Komponente die Temperatur und den Wetterstatus korrekt anzeigt, wenn sie mit Beispielwerten initialisiert wird.  

// Nutze für den zweiten Test Jest's Mocking-Funktionen, um die Werte für Temperatur und Wetterstatus zu simulieren.